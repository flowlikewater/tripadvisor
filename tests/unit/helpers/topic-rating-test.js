
import { topicRating } from 'tripadvisor/helpers/topic-rating';
import { module, test } from 'qunit';

module('Unit | Helper | topic rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = topicRating([42]);
  assert.ok(result);
});

